server:
  port: 8080

spring:
  application:
     name:  home-controller
  freemarker:
    suffix: .ftl

 

feign:
  client:
     config:
       default:
        loggerLevel: basic
        connectTimeout: 1600000000
        readTimeout: 1600000000
        logger-level: FULL 

feign.compression.request.enabled: true
feign.compression.response.enabled: true
logging.level.frontend.service.ApiGatewayRequestUri: DEBUG

