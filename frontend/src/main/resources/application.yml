spring:
  application:
     name:  home-controller
  freemarker:
    suffix: .ftl

server:
  port: 8080

hystrix:
   command:
      default:
         execution:
            iso lation:
               thread:
                  timeoutInMilliseconds: 30000

eureka:
  client:
    serviceUrl:
        defaultZone: http://localhost.herokuapp.com/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10
    statusPageUrlPath: /info
    healthCheckUrlPath: /health

feign:
  client:
    config:
      default:
        connectTimeout: 120000
        readTimeout: 120000
